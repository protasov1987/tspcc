<!DOCTYPE html>
<html lang="ru">
<head>
  <meta charset="utf-8" />
  <title>QR-код группы карт</title>
  <style>
    @page { margin: 10mm; }
    body { font-family: system-ui, -apple-system, BlinkMacSystemFont, "Segoe UI", sans-serif; text-align: center; margin: 0; padding: 0; }
    .barcode-wrapper { display: inline-flex; flex-direction: column; align-items: center; gap: 12px; padding: 5mm; margin: 5mm auto 0; }
    .barcode-wrapper .barcode-svg { padding: 5mm; }
    .barcode-wrapper svg { width: 32mm; height: 32mm; display: block; }
    .code-text { font-size: 16px; font-weight: 600; }
  </style>
</head>
<body>
  <div class="barcode-wrapper">
    <div class="barcode-svg"><%- barcodeSvg %></div>
    <div class="code-text">QR ID группы: <%= code || '(нет QR ID группы)' %></div>
    <div class="code-text">Группа: <%= card && card.name ? escapeHtml(card.name) : '' %></div>
  </div>
  <script>
    (function () {
      let fired = false;
      window.addEventListener('load', () => {
        if (fired) return;
        fired = true;
        setTimeout(() => window.print(), 250);
      });
      window.addEventListener('afterprint', () => {
        window.close();
      });
    })();
  </script>
</body>
</html>
